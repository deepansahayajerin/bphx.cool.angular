/**
 *  A field insensity.
 */
export const enum Intensity
{
  /**
   * A field is displayed at normal intensity.
   */
  Normal = "normal",
  /**
   * A field is displayed at brighter than normal intensity.
   */
  High = "high",
  /**
   * A field is not displayed.
   */
  Dark = "dark"
}
