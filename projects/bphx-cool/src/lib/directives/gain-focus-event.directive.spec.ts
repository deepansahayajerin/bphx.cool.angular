import { GainFocusEventDirective } from "./gain-focus-event.directive";

describe("GainFocusEventDirective", () => {
  // it('should create an instance', () => {
  //   const directive = new GainFocusDirective();
  //   expect(directive).toBeTruthy();
  // });
});
