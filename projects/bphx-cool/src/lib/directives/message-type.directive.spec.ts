import { MessageTypeDirective } from "./message-type.directive";

describe("MessageTypeDirective", () =>
{
  it("should create an instance", () =>
  {
    const directive = new MessageTypeDirective();
    expect(directive).toBeTruthy();
  });
});
