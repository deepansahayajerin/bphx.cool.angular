import { ChangedDirective } from "./changed-event.directive";

describe("ChangedEventDirective", () => {
  // it("should create an instance", () => {
  //   const directive = new ChangedDirective();
  //   expect(directive).toBeTruthy();
  // });
});
