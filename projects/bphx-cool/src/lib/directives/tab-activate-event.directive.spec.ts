import { TabActivateEventDirective } from './tab-activate-event.directive';

describe('TabActivateEventDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new TabActivateDirective();
  //   expect(directive).toBeTruthy();
  // });
});
