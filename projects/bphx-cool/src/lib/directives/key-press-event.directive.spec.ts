import { KeyPressDirective } from './key-press-event.directive';

describe('KeyPressEventDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new KeyPressDirective();
  //   expect(directive).toBeTruthy();
  // });
});
