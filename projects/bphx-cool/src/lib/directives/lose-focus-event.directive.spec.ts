import { LoseFocusEventDirective } from './lose-focus-event.directive';

describe('LoseFocusEventDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new LoseFocusDirective();
  //   expect(directive).toBeTruthy();
  // });
});
